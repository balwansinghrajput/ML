
1. **Scikit-learn Introduction**

   * sklearn kya hai
   * ML workflow
   * Estimator, Transformer, Predictor

2. **Core Methods**

   * `fit()`
   * `transform()`
   * `fit_transform()`
   * `predict()`
   * `predict_proba()`

3. **Features & Target**

   * `X` and `y`
   * input features
   * target variable

4. **Train-Test Split**

   * `train_test_split()`
   * `test_size`
   * `random_state`
   * `shuffle`
   * `stratify`

5. **Missing Value Handling**

   * `SimpleImputer`
   * `KNNImputer`
   * advanced imputation

6. **Categorical Encoding**

   * `LabelEncoder`
   * `OrdinalEncoder`
   * `OneHotEncoder`

7. **Feature Scaling**

   * `StandardScaler`
   * `MinMaxScaler`
   * `RobustScaler`
   * `Normalizer`
   * scaler kab use karna hai

8. **ColumnTransformer**

9. **Pipeline & make_pipeline**

10. **Regression**

* Linear Regression
* Polynomial Regression
* Ridge
* Lasso
* ElasticNet

11. **Regression Metrics**

* MAE
* MSE
* RMSE
* R²

12. **Classification**

* Logistic Regression
* KNN
* Decision Tree
* Random Forest
* SVM
* Naive Bayes
* SGDClassifier

13. **Classification Metrics**

* Confusion Matrix
* Accuracy
* Precision
* Recall
* F1
* ROC-AUC

14. **Cross Validation**

* KFold
* StratifiedKFold
* `cross_val_score()`
* TimeSeriesSplit

15. **Hyperparameter Tuning**

* GridSearchCV
* RandomizedSearchCV

16. **Feature Selection**

* SelectKBest
* RFE
* RFECV
* SelectFromModel

17. **PCA / Dimensionality Reduction**

18. **Ensemble Learning**

* Bagging
* Random Forest
* AdaBoost
* Gradient Boosting
* Voting
* Stacking

19. **Clustering**

* KMeans
* DBSCAN
* Agglomerative Clustering

20. **Outlier Detection**

* IsolationForest
* LocalOutlierFactor
* OneClassSVM

21. **Imbalanced Dataset Handling**

22. **Advanced Sklearn API**

* `get_params()`
* `set_params()`
* `named_steps`
* `named_transformers_`
* `get_feature_names_out()`

23. **Custom Transformers**

24. **Data Leakage**

25. **Model Saving**

* `joblib.dump()`
* `joblib.load()`

26. **Complete End-to-End ML Project**

27. **Sklearn + FastAPI Deployment**
